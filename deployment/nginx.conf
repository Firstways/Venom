events {}

http {
    server {
        listen 80;

        location / {
            root /usr/share/nginx/html;
            index promozone.html;
            add_header X-Custom-Header "DLLHERE";
        }
    }
}